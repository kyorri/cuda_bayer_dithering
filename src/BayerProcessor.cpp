#include "BayerProcessor.h"

#include <algorithm>

namespace bayer_dithering {
	void BayerProcessor::ProcessImages(std::vector<Image&> images) {
		for (auto& image : images) {
        
		}
	};
} // namespace bayer_dithering